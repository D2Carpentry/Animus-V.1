Upload both files to the ANIMUS repository and replace the existing copies.

This removes the old-interface flash when Command Center opens. The page now
shows a clean ANIMUS opening screen until the current interface is ready.

After deployment, use:
https://animus-v-1.pages.dev/crm?smoothOpening=20260823
