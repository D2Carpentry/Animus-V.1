Upload all three items in this folder to the ANIMUS repository, keeping the folders exactly as shown:

1. crm.html
2. expenses-v6.js
3. functions/api/expenses.js

After Cloudflare finishes deploying, every newly saved receipt photo is stored as its own file in ANIMUS cloud storage. The expense record keeps a link to that photo, so the main Command Center backup stays small.

Existing receipt photos are left alone. They will continue to display. New photos use the separate receipt-photo backup area.
