Upload and replace every file in this folder in the ANIMUS repository root.

This package fixes the main Save button so it cannot save an empty or partial
Revenue list over the verified revenue history.

Included revenue recovery:
- 40 verified historical revenue records
- $101,476.00 gross revenue
- $25,050.67 expenses

The recovery is revenue-only. It does not contain customer file records,
work-file statuses, estimates, expenses, or receipts.

Also included are the current shared sidebar and Payroll files so the updated
crm.html always has every script it needs.

After upload, open:
https://animus-v-1.pages.dev/crm?revenueSafeSave=20260822

Then confirm the Revenue page has the historical rows before clicking Save.
Once confirmed, click Save once. The new protection sends the complete ledger
to Cloudflare instead of allowing a blank list to replace it.
