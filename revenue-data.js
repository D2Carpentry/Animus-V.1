window.D2_REVENUE_ROWS = [
  { id: "rev-sheet-1", date: "2026-01-16", clientJob: "Fredman - Church", gross: 1250, expenses: 0, labor: 0, profit: 1250, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-2", date: "2026-01-23", clientJob: "Fredman - Church", gross: 1250, expenses: 0, labor: 0, profit: 1250, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-3", date: "2026-01-30", clientJob: "Fredman - Church", gross: 1250, expenses: 0, labor: 0, profit: 1250, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-4", date: "2026-02-06", clientJob: "Fredman - Church", gross: 1250, expenses: 0, labor: 0, profit: 1250, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-5", date: "2026-02-13", clientJob: "Fredman - Church", gross: 1250, expenses: 0, labor: 0, profit: 1250, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-6", date: "2026-02-20", clientJob: "Fredman - Church", gross: 1250, expenses: 0, labor: 0, profit: 1250, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-7", date: "2026-02-27", clientJob: "Fredman - Church", gross: 1250, expenses: 0, labor: 0, profit: 1250, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-8", date: "2026-03-06", clientJob: "Fredman - Church", gross: 1250, expenses: 0, labor: 0, profit: 1250, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-9", date: "2026-01-09", clientJob: "Fredman - Church", gross: 1250, expenses: 0, labor: 0, profit: 1250, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-10", date: "2026-03-03", clientJob: "Mike-Misc", gross: 100, expenses: 0, labor: 0, profit: 100, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-11", date: "2026-03-03", clientJob: "Mike-Misc", gross: 520, expenses: 0, labor: 0, profit: 520, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-12", date: "2026-03-03", clientJob: "Mike-Misc", gross: 500, expenses: 0, labor: 0, profit: 500, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-13", date: "2026-03-03", clientJob: "Mike-Misc", gross: 350, expenses: 0, labor: 0, profit: 350, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-14", date: "2026-03-15", clientJob: "Mike-Misc", gross: 1200, expenses: 0, labor: 0, profit: 1200, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-15", date: "2026-03-21", clientJob: "Brian", gross: 4000, expenses: 1500, labor: 0, profit: 2500, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-16", date: "2026-03-22", clientJob: "Fredman - Church", gross: 750, expenses: 0, labor: 0, profit: 750, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-17", date: "2026-03-24", clientJob: "Deb-Misc", gross: 1002, expenses: 0, labor: 0, profit: 1002, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-18", date: "2026-03-23", clientJob: "Diane-Misc", gross: 40, expenses: 0, labor: 0, profit: 40, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-19", date: "2026-03-25", clientJob: "Liz-Misc", gross: 100, expenses: 0, labor: 0, profit: 100, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-20", date: "2026-03-25", clientJob: "David Neville -Drywall", gross: 50, expenses: 0, labor: 0, profit: 50, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-21", date: "2026-03-26", clientJob: "Brian-Misc", gross: 450, expenses: 0, labor: 0, profit: 450, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-22", date: "2026-04-07", clientJob: "Donna - Slide Outs", gross: 2990, expenses: 990, labor: 0, profit: 2000, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-23", date: "2026-04-25", clientJob: "Bob-Planks", gross: 2290, expenses: 290, labor: 0, profit: 2000, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-24", date: "2026-05-05", clientJob: "Jake - Fascia Board", gross: 596, expenses: 0, labor: 0, profit: 596, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-25", date: "2026-05-08", clientJob: "Jake - Fascia Board", gross: 350, expenses: 0, labor: 0, profit: 350, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-26", date: "2026-05-18", clientJob: "Jim Goodman-Cabinet Staining", gross: 600, expenses: 282.33, labor: 0, profit: 317.67, receiptNotes: "Home Depot $70.74\nHome Depot $90.23\nAmazon: $62.82\nAmazon: $19.16\nAmazon: $23.42\nAmazon: $15.96\nSherwin W: $64.40", laborAssigns: "" },
  { id: "rev-sheet-27", date: "2026-05-25", clientJob: "Laurie - Concrete Counters", gross: 3162, expenses: 393.72, labor: 1000, profit: 1768.28, receiptNotes: "SW: $18.09\nHD: $364.99\nAmazon: $10.64", laborAssigns: "Nesto" },
  { id: "rev-sheet-28", date: "2026-06-01", clientJob: "Deb-Fan and Lights", gross: 542, expenses: 272.04, labor: 0, profit: 269.96, receiptNotes: "", laborAssigns: "" },
  { id: "rev-sheet-29", date: "2026-06-11", clientJob: "Ana - Abi Closet", gross: 1850, expenses: 573.62, labor: 0, profit: 1276.38, receiptNotes: "IMECA-$237.96 + 72.85\nHD: $38.04 + 7.43 + 11.98\nAmazon: $125.57\nSW: 54.78", laborAssigns: "" },
  { id: "rev-sheet-30", date: "", clientJob: "Reese-Pantry/Console/Closets", gross: 7500, expenses: 765.16, labor: 1250, profit: 5484.84, receiptNotes: "Tools: 656.19\nHD: 74.28\nAmazon: 34.69", laborAssigns: "" },
  { id: "rev-sheet-31", date: "", clientJob: "Brian- Welks", gross: 0, expenses: 12.34, labor: 0, profit: -12.34, receiptNotes: "SW: 12.34 (Paint Sample)", laborAssigns: "" },
];

window.addEventListener("load", () => {
  if (window.D2_EXPENSE_REPAIR_READY) return;
  window.D2_EXPENSE_REPAIR_READY = true;

  const taxRate = typeof DEFAULT_EXPENSE_TAX_RATE === "number" ? DEFAULT_EXPENSE_TAX_RATE : 0.065;
  const money = (value) => (typeof crmCurrency !== "undefined" ? crmCurrency.format(Number(value) || 0) : `$${(Number(value) || 0).toFixed(2)}`);
  const id = (prefix) => (typeof makeCrmId === "function" ? makeCrmId(prefix) : `${prefix}-${Date.now()}-${Math.random().toString(16).slice(2)}`);
  const today = () => (typeof todayIso === "function" ? todayIso(0) : new Date().toISOString().slice(0, 10));
  const amount = (value) => (typeof parseMoney === "function" ? parseMoney(value) : Number(String(value || "").replace(/[^0-9.-]/g, "")) || 0);
  const html = (value) => (typeof escapeHtml === "function" ? escapeHtml(value) : String(value || "").replace(/[&<>"']/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "\"": "&quot;", "'": "&#39;" }[char])));

  function baseCategory(category = "Supplies", customCategory = "") {
    return category === "Other" ? (customCategory || "Other") : (category || "Supplies");
  }

  function cleanLine(line = {}) {
    return {
      id: line.id || id("expense-line"),
      use: line.use !== false,
      description: line.description || line.note || "",
      category: line.category || "Supplies",
      price: line.price === undefined ? (line.baseAmount || line.amount || "") : line.price,
      addTax: line.addTax !== false,
      taxRate: Number(line.taxRate) || taxRate,
    };
  }

  function lineTotal(line = {}) {
    if (line.use === false) return 0;
    const base = amount(line.price);
    return line.addTax ? base + (base * (Number(line.taxRate) || taxRate)) : base;
  }

  function cleanReceipt(receipt = {}) {
    return {
      id: receipt.id || id("fresh-receipt"),
      createdAt: receipt.createdAt || new Date().toISOString(),
      updatedAt: receipt.updatedAt || receipt.createdAt || new Date().toISOString(),
      date: receipt.date || today(),
      vendor: receipt.vendor || "",
      category: receipt.category || "Supplies",
      customCategory: receipt.customCategory || "",
      receiptTotal: receipt.receiptTotal === undefined ? "" : receipt.receiptTotal,
      paymentType: receipt.paymentType || "",
      paymentCard: receipt.paymentCard || "",
      imageTitle: receipt.imageTitle || receipt.receiptFileName || receipt.fileName || "",
      fileName: receipt.fileName || receipt.receiptFileName || "",
      imageDataUrl: receipt.imageDataUrl || receipt.receiptDataUrl || "",
      notes: receipt.notes || "",
      pastedText: receipt.pastedText || "",
      lines: (Array.isArray(receipt.lines) ? receipt.lines : []).map(cleanLine),
    };
  }

  function receiptTotal(receipt = {}) {
    return (Array.isArray(receipt.lines) ? receipt.lines : []).reduce((sum, line) => sum + lineTotal(line), 0);
  }

  function receiptsForFile(file) {
    if (!file) return [];
    file.freshExpenseReceipts = Array.isArray(file.freshExpenseReceipts) ? file.freshExpenseReceipts.map(cleanReceipt) : [];
    return file.freshExpenseReceipts;
  }

  function rebuild(file) {
    if (!file) return;
    const receipts = receiptsForFile(file);
    file.expenseLines = receipts.flatMap((receipt) => {
      return receipt.lines.filter((line) => line.use !== false).map((line, index) => {
        const base = amount(line.price);
        const rate = Number(line.taxRate) || taxRate;
        const tax = line.addTax ? base * rate : 0;
        return {
          id: line.id || id("expense"),
          receiptGroupId: receipt.id,
          date: receipt.date || today(),
          category: line.category || baseCategory(receipt.category, receipt.customCategory),
          vendor: receipt.vendor || "",
          note: line.description || receipt.notes || receipt.imageTitle || "Receipt expense",
          baseAmount: base,
          amount: base + tax,
          tax,
          addTax: Boolean(line.addTax),
          taxRate: rate,
          paymentType: [receipt.paymentType, receipt.paymentCard].filter(Boolean).join(" - "),
          receiptFileName: receipt.imageTitle || receipt.fileName || "",
          receiptDataUrl: index === 0 ? (receipt.imageDataUrl || "") : "",
          receiptSource: "Saved receipt",
        };
      });
    });
    file.expenseReceipts = receipts.map((receipt) => ({ ...receipt, lines: receipt.lines.map((line) => ({ ...line })) }));
    if (typeof receiptHistoryEntryFromLines === "function") {
      file.receiptHistory = receipts.map((receipt) => receiptHistoryEntryFromLines(receipt.id, file.expenseLines.filter((line) => line.receiptGroupId === receipt.id), {
        savedAt: receipt.createdAt,
        updatedAt: receipt.updatedAt,
        label: receipt.imageTitle || receipt.vendor || receipt.notes || "Saved receipt",
        date: receipt.date,
        vendor: receipt.vendor,
        category: baseCategory(receipt.category, receipt.customCategory),
        paymentType: [receipt.paymentType, receipt.paymentCard].filter(Boolean).join(" - "),
      }));
    }
  }

  function captureDraft() {
    if (!$("crmFileReceiptReview") || $("crmFileReceiptReview").hidden) return;
    fileReceiptDraft.vendor = $("crmFileReceiptVendor")?.value.trim() || "";
    fileReceiptDraft.date = $("crmFileReceiptDate")?.value || "";
    fileReceiptDraft.category = $("crmFileReceiptCategory")?.value || "Supplies";
    fileReceiptDraft.customCategory = $("crmFileReceiptOtherCategory")?.value.trim() || "";
    fileReceiptDraft.amount = $("crmFileReceiptAmount")?.value || "";
    fileReceiptDraft.paymentType = $("crmFileReceiptPayment")?.value || "";
    fileReceiptDraft.paymentCard = $("crmFileReceiptCard")?.value || "";
    fileReceiptDraft.imageTitle = $("crmFileReceiptImageTitle")?.value.trim() || "";
    fileReceiptDraft.notes = $("crmFileReceiptNotes")?.value.trim() || "";
    fileReceiptDraft.pastedText = $("crmFileReceiptOcrText")?.value.trim() || "";
    if (typeof captureFileReceiptLineRows === "function") captureFileReceiptLineRows();
  }

  function draftReceipt(receiptId = id("fresh-receipt")) {
    const category = baseCategory(fileReceiptDraft.category, fileReceiptDraft.customCategory);
    const usable = typeof usableReceiptDraftLines === "function"
      ? usableReceiptDraftLines(fileReceiptDraft)
      : (Array.isArray(fileReceiptDraft.lines) ? fileReceiptDraft.lines : []).filter((line) => line.use !== false && (line.description || amount(line.price)));
    const lines = usable.map((line) => cleanLine({
      ...line,
      category: line.category || category,
      taxRate: typeof receiptTaxRateForDraft === "function" ? receiptTaxRateForDraft(fileReceiptDraft) : taxRate,
    }));
    if (!lines.length && (amount(fileReceiptDraft.amount) || fileReceiptDraft.vendor || fileReceiptDraft.notes || fileReceiptDraft.imageDataUrl)) {
      lines.push(cleanLine({
        description: fileReceiptDraft.notes || fileReceiptDraft.imageTitle || fileReceiptDraft.fileName || "Receipt expense",
        category,
        price: fileReceiptDraft.amount || "",
        addTax: false,
      }));
    }
    return cleanReceipt({
      id: receiptId,
      createdAt: fileReceiptDraft.createdAt || new Date().toISOString(),
      updatedAt: new Date().toISOString(),
      date: fileReceiptDraft.date || today(),
      vendor: fileReceiptDraft.vendor || "",
      category: fileReceiptDraft.category || "Supplies",
      customCategory: fileReceiptDraft.customCategory || "",
      receiptTotal: fileReceiptDraft.amount || "",
      paymentType: fileReceiptDraft.paymentType || "",
      paymentCard: fileReceiptDraft.paymentCard || "",
      imageTitle: fileReceiptDraft.imageTitle || fileReceiptDraft.fileName || "",
      fileName: fileReceiptDraft.fileName || "",
      imageDataUrl: fileReceiptDraft.imageDataUrl || "",
      notes: fileReceiptDraft.notes || "",
      pastedText: fileReceiptDraft.pastedText || "",
      lines,
    });
  }

  function syncAndSave(file, message = "Expense saved to Cloudflare.") {
    rebuild(file);
    if (typeof syncFileExpensesToRevenue === "function") syncFileExpensesToRevenue(file);
    if (typeof saveCrmFiles === "function") saveCrmFiles();
    if (typeof saveRevenueRows === "function") saveRevenueRows();
    renderFileExpenses();
    if (typeof saveExpenseChangeToCloud === "function") saveExpenseChangeToCloud(message);
  }

  function renderHistory(file) {
    const list = $("crmReceiptHistoryList");
    if (!list) return;
    if (!file) {
      list.innerHTML = `<p class="crm-empty-state">Select a file to see saved receipts and expenses.</p>`;
      return;
    }
    const receipts = receiptsForFile(file);
    if (!receipts.length) {
      list.innerHTML = `<p class="crm-empty-state">No saved receipts or expenses yet.</p>`;
      return;
    }
    list.innerHTML = receipts.map((receipt) => {
      const lineCount = receipt.lines.length;
      const meta = [receipt.date || today(), receipt.vendor || "", `${lineCount} line${lineCount === 1 ? "" : "s"}`, [receipt.paymentType, receipt.paymentCard].filter(Boolean).join(" - ")].filter(Boolean).join(" · ");
      return `<button type="button" class="crm-receipt-history-item" data-fresh-receipt-open="${html(receipt.id)}"><span><strong>${html(receipt.imageTitle || receipt.vendor || receipt.notes || "Saved receipt")}</strong><small>${html(meta)}</small></span><b>${money(receiptTotal(receipt))}</b></button>`;
    }).join("");
    list.querySelectorAll("[data-fresh-receipt-open]").forEach((button) => button.addEventListener("click", () => openReceipt(button.dataset.freshReceiptOpen)));
  }

  function freshRenderFileExpenses() {
    const file = typeof activeFile === "function" && typeof normalizeCrmFile === "function" ? normalizeCrmFile(activeFile()) : null;
    const rows = $("crmFileExpenseRows");
    if (!rows) return;
    if (!file) {
      if ($("crmExpensesFileTitle")) $("crmExpensesFileTitle").textContent = "Select a file to track expenses.";
      if ($("crmExpensesHeading")) $("crmExpensesHeading").textContent = "No file selected";
      if ($("crmFileExpenseTotal")) $("crmFileExpenseTotal").textContent = money(0);
      rows.innerHTML = `<tr><td colspan="9">No file selected.</td></tr>`;
      fileReceiptDraft = typeof blankFileReceiptDraft === "function" ? blankFileReceiptDraft() : {};
      if (typeof renderFileReceiptDraft === "function") renderFileReceiptDraft();
      renderHistory(null);
      return;
    }
    rebuild(file);
    if ($("crmExpensesFileTitle")) $("crmExpensesFileTitle").textContent = `${file.fileNumber || "Project"} · ${file.clientName || "Unnamed Client"}`;
    if ($("crmExpensesHeading")) $("crmExpensesHeading").textContent = file.clientName || "Unnamed Client";
    if ($("crmFileExpenseTotal")) $("crmFileExpenseTotal").textContent = money(file.expenseLines.reduce((sum, line) => sum + (typeof receiptExpenseLineAmount === "function" ? receiptExpenseLineAmount(line) : Number(line.amount) || 0), 0));
    rows.innerHTML = file.expenseLines.map((line) => {
      const base = typeof expenseLineBaseAmount === "function" ? expenseLineBaseAmount(line) : Number(line.baseAmount) || 0;
      const tax = typeof expenseLineTaxAmount === "function" ? expenseLineTaxAmount(line) : Number(line.tax) || 0;
      const total = typeof receiptExpenseLineAmount === "function" ? receiptExpenseLineAmount(line) : Number(line.amount) || 0;
      const notePreview = line.note ? line.note.split(/\s+/).slice(0, 5).join(" ") : "Add details";
      return `<tr><td><input class="crm-revenue-input" type="date" value="${html(line.date || today())}"></td><td>${html(line.category || "Supplies")}</td><td>${html(line.vendor || "")}</td><td><button type="button" class="crm-expense-note-button" data-file-expense-note="${html(line.id)}"><span>${html(notePreview)}</span></button></td><td>${money(base)}</td><td class="crm-expense-tax-toggle">${line.addTax ? `Yes <small>${money(tax)}</small>` : "No"}</td><td><strong>${money(total)}</strong></td><td><button type="button" data-fresh-expense-open="${html(line.receiptGroupId || line.id)}">${line.receiptDataUrl ? "Open" : "Edit"}</button></td><td><button type="button" data-fresh-expense-delete="${html(line.receiptGroupId || line.id)}">Delete</button></td></tr>`;
    }).join("") || `<tr><td colspan="9">No expenses added yet.</td></tr>`;
    if (typeof renderFileReceiptDraft === "function") renderFileReceiptDraft();
    renderHistory(file);
    rows.querySelectorAll("[data-file-expense-note]").forEach((button) => button.addEventListener("click", () => {
      if (typeof openExpenseNoteModal === "function") openExpenseNoteModal(button.dataset.fileExpenseNote);
    }));
    rows.querySelectorAll("[data-fresh-expense-open]").forEach((button) => button.addEventListener("click", () => openReceipt(button.dataset.freshExpenseOpen)));
    rows.querySelectorAll("[data-fresh-expense-delete]").forEach((button) => button.addEventListener("click", () => deleteReceipt(button.dataset.freshExpenseDelete)));
  }

  function openReceipt(receiptId) {
    const file = typeof activeFile === "function" && typeof normalizeCrmFile === "function" ? normalizeCrmFile(activeFile()) : null;
    const receipt = receiptsForFile(file).find((entry) => entry.id === receiptId);
    if (!receipt || typeof blankFileReceiptDraft !== "function") return;
    fileReceiptDraft = {
      ...blankFileReceiptDraft(),
      imageDataUrl: receipt.imageDataUrl || "",
      fileName: receipt.fileName || "",
      imageTitle: receipt.imageTitle || receipt.fileName || "",
      vendor: receipt.vendor || "",
      date: receipt.date || today(),
      category: ["Supplies", "Materials", "Fuel", "Equipment", "Other"].includes(receipt.category) ? receipt.category : "Other",
      customCategory: receipt.customCategory || (["Supplies", "Materials", "Fuel", "Equipment", "Other"].includes(receipt.category) ? "" : receipt.category),
      amount: receipt.receiptTotal || receiptTotal(receipt).toFixed(2),
      paymentType: receipt.paymentType || "",
      paymentCard: receipt.paymentCard || "",
      notes: receipt.notes || "",
      pastedText: receipt.pastedText || "",
      lines: receipt.lines.map((line) => (typeof blankFileReceiptLine === "function" ? blankFileReceiptLine(line) : cleanLine(line))),
      isEditingSavedReceipt: true,
      editingReceiptGroupId: receipt.id,
      createdAt: receipt.createdAt || new Date().toISOString(),
      status: "Editing a saved receipt. Save Expense will update this receipt.",
    };
    if (typeof renderFileReceiptDraft === "function") renderFileReceiptDraft();
    $("crmFileReceiptReview")?.scrollIntoView({ behavior: "smooth", block: "start" });
  }

  function deleteReceipt(receiptId) {
    const file = typeof activeFile === "function" && typeof normalizeCrmFile === "function" ? normalizeCrmFile(activeFile()) : null;
    if (!file || !receiptId) return;
    file.freshExpenseReceipts = receiptsForFile(file).filter((receipt) => receipt.id !== receiptId);
    if (typeof addSystemNote === "function") addSystemNote(file, "Expense receipt deleted.");
    syncAndSave(file, "Expense deleted and saved to Cloudflare.");
  }

  function addManualDraft() {
    if (typeof activeFile !== "function" || !activeFile()) {
      window.alert("Select a customer file before adding an expense.");
      return;
    }
    fileReceiptDraft = {
      ...(typeof blankFileReceiptDraft === "function" ? blankFileReceiptDraft() : {}),
      date: today(),
      category: "Supplies",
      lines: [typeof blankFileReceiptLine === "function" ? blankFileReceiptLine({ category: "Supplies", addTax: false }) : cleanLine({ addTax: false })],
      status: "Add the expense details, then click Save Expense.",
    };
    if (typeof renderFileReceiptDraft === "function") renderFileReceiptDraft();
    $("crmFileReceiptReview")?.scrollIntoView({ behavior: "smooth", block: "start" });
  }

  async function attachReceipt(files = []) {
    const upload = Array.from(files || [])[0];
    if (!upload) return;
    if (Array.from(files || []).length > 1) window.alert("Upload one receipt at a time for now. Save it, then upload the next receipt.");
    if (typeof showReceiptLoading === "function") showReceiptLoading("Reading receipt photo...");
    try {
      const imageDataUrl = typeof readUploadFileAsDataUrl === "function" ? await readUploadFileAsDataUrl(upload) : "";
      let nextDraft = {
        ...(typeof blankFileReceiptDraft === "function" ? blankFileReceiptDraft() : {}),
        imageDataUrl,
        fileName: upload.name || "",
        imageTitle: upload.name || "Receipt photo",
        date: today(),
        category: "Supplies",
        lines: [],
        status: "Receipt photo attached. Review the fields, then save the expense.",
      };
      try {
        if (typeof readFileReceiptWithAi === "function" && typeof receiptResultToDraft === "function") {
          const result = await readFileReceiptWithAi(imageDataUrl, upload);
          nextDraft = { ...receiptResultToDraft(result, nextDraft), imageDataUrl, fileName: upload.name || "", imageTitle: upload.name || "Receipt photo" };
        }
      } catch (error) {
        nextDraft.status = "Receipt photo attached. AI reading did not finish, so review or paste receipt text before saving.";
      }
      if (!Array.isArray(nextDraft.lines) || !nextDraft.lines.length) {
        nextDraft.lines = [typeof blankFileReceiptLine === "function" ? blankFileReceiptLine({ category: "Supplies", addTax: false }) : cleanLine({ addTax: false })];
      }
      fileReceiptDraft = nextDraft;
      if (typeof renderFileReceiptDraft === "function") renderFileReceiptDraft();
    } finally {
      if (typeof hideReceiptLoading === "function") hideReceiptLoading();
    }
  }

  function addReceiptLine() {
    captureDraft();
    fileReceiptDraft.lines = Array.isArray(fileReceiptDraft.lines) ? fileReceiptDraft.lines : [];
    fileReceiptDraft.lines.push(typeof blankFileReceiptLine === "function" ? blankFileReceiptLine({ category: fileReceiptDraft.category || "Supplies", addTax: false }) : cleanLine({ addTax: false }));
    if (typeof renderFileReceiptLineRows === "function") renderFileReceiptLineRows();
  }

  function clearDraft() {
    fileReceiptDraft = typeof blankFileReceiptDraft === "function" ? blankFileReceiptDraft() : {};
    if (typeof renderFileReceiptDraft === "function") renderFileReceiptDraft();
    if (typeof setFileReceiptStatus === "function") setFileReceiptStatus("Receipt cleared.", "warn");
  }

  function saveReceipt() {
    const file = typeof activeFile === "function" && typeof normalizeCrmFile === "function" ? normalizeCrmFile(activeFile()) : null;
    if (!file) {
      window.alert("Select a customer file before saving a receipt.");
      return;
    }
    captureDraft();
    const isEditing = Boolean(fileReceiptDraft.isEditingSavedReceipt && fileReceiptDraft.editingReceiptGroupId);
    const receiptId = isEditing ? fileReceiptDraft.editingReceiptGroupId : id("fresh-receipt");
    const receipt = draftReceipt(receiptId);
    if (!receipt.lines.length) {
      if (typeof setFileReceiptStatus === "function") setFileReceiptStatus("Add receipt details before saving.", "warn");
      return;
    }
    file.freshExpenseReceipts = [{ ...receipt, updatedAt: new Date().toISOString() }, ...receiptsForFile(file).filter((entry) => entry.id !== receiptId)];
    if (typeof addSystemNote === "function") addSystemNote(file, `Receipt expense ${isEditing ? "updated" : "saved"}${receipt.vendor ? ` from ${receipt.vendor}` : ""} for ${money(receiptTotal(receipt))}.`);
    fileReceiptDraft = typeof blankFileReceiptDraft === "function" ? blankFileReceiptDraft() : {};
    syncAndSave(file, "Receipt saved to Cloudflare.");
    if (typeof setFileReceiptStatus === "function") setFileReceiptStatus("Receipt saved to this file.", "good");
  }

  try {
    renderFileExpenses = freshRenderFileExpenses;
  } catch (error) {
    window.renderFileExpenses = freshRenderFileExpenses;
  }

  const captureClick = (idValue, handler) => {
    const element = $(idValue);
    if (!element) return;
    element.addEventListener("click", (event) => {
      event.preventDefault();
      event.stopImmediatePropagation();
      handler(event);
    }, true);
  };
  captureClick("crmAddFileExpense", addManualDraft);
  captureClick("crmSaveScannedReceipt", saveReceipt);
  captureClick("crmClearScannedReceipt", clearDraft);
  captureClick("crmAddReceiptExpenseLine", addReceiptLine);
  captureClick("crmSaveAllReceipts", () => window.alert("Bulk receipt upload is paused while we stabilize expenses. Upload one receipt at a time for now."));

  const uploadInput = $("crmReceiptUpload");
  if (uploadInput) {
    uploadInput.addEventListener("change", (event) => {
      event.stopImmediatePropagation();
      attachReceipt(event.target.files);
      event.target.value = "";
    }, true);
  }

  [
    "crmFileReceiptVendor",
    "crmFileReceiptDate",
    "crmFileReceiptCategory",
    "crmFileReceiptOtherCategory",
    "crmFileReceiptAmount",
    "crmFileReceiptPayment",
    "crmFileReceiptCard",
    "crmFileReceiptImageTitle",
    "crmFileReceiptNotes",
    "crmFileReceiptOcrText",
  ].forEach((fieldId) => {
    const field = $(fieldId);
    if (!field) return;
    ["input", "change"].forEach((eventName) => {
      field.addEventListener(eventName, (event) => {
        event.stopImmediatePropagation();
        captureDraft();
        if (typeof toggleFileReceiptConditionalFields === "function") toggleFileReceiptConditionalFields();
      }, true);
    });
  });

  if (!$("crmExpensesView")?.hidden) freshRenderFileExpenses();
});
